name: Production Guard

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  enforce-ca:
    uses: beckyshaw/change-window-policy/.github/workflows/enforce-prod-change-assurance.yml@v1
